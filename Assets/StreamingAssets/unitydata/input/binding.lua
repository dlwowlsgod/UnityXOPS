-- ============================================================
--  입력 바인딩 모드 스크립트 (binding.lua)
--  InputManager가 기본 액션을 빌드한 직후 1회 실행된다. 여기서는 추가 입력 액션을 등록한다.
--
--  [ 액션 추가 ]
--    버튼(누름/뗌) 액션:  XOPS.Input:RegisterButton(이름, 바인딩경로)
--    축(연속 값) 액션:    XOPS.Input:RegisterAxis(이름, 바인딩경로)
--    바인딩 경로 예시: "<Keyboard>/leftShift", "<Mouse>/leftButton", "<Mouse>/scroll/y"
--
--  전체 API 레퍼런스(폴링/UI/카메라 등)는 별도 매뉴얼 문서 참고.
-- ============================================================


XOPS.Debug:Log("모든 액션 등록 완료")